[project]
name = "owilx-oracle"
version = "0.1.0"
dependencies = [
    "python-dotenv",
    "pydantic-settings",
    "openai",
    "httpx",
    "playwright"
]

[project.scripts]
owilx-sports = "sports.cli:app"

[project.optional-dependencies]
playwright = ["playwright"]
