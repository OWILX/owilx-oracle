[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sports-betting"
version = "0.1.0"
dependencies = [
    "python-dotenv",
    "pydantic-settings",
    "openai",
    "httpx"
]

[project.scripts]
owilx-sports = "sports.cli:app"
